# Auditoría de librería — 2026-09-22

Motivo: Top Gun Maverick se reprodujo con títulos en muchos idiomas. Pedido: auditoría
completa de las 341 películas + revisión de los 117 upgrades de Radarr + causa.

## 1. Cifras generales

| status | n | qué significa |
|---|---|---|
| `clean` | 325 | remuxeada y verificada, o aceptada tal cual (`/accept`) |
| `ready` | 15 | plan de pistas calculado (`suggest_tracks`), **remux nunca lanzado** — el fichero en disco sigue siendo el crudo de Radarr, con todos sus idiomas y sin default fijado |
| `stub` | 1 | sin fichero |

Top Gun Maverick es uno de los 15 `ready`. Su plan en base de datos es correcto
(inglés AC-3 default, español AC-3, subtítulo es-mx) — el problema no es la política,
es que ese plan nunca se aplicó al fichero.

## 2. Los 15 `ready` (nunca remuxeados)

| película | jobs previos | origen |
|---|---|---|
| Top Gun Maverick (2022) | ninguno | Radarr, import 09-14 |
| Sonic the Hedgehog 2 (2022) | ninguno | Radarr, import 09-11 |
| The Northman (2022) | ninguno | Radarr, import 09-14 |
| 2 Fast 2 Furious (2003) | remux verificado 09-02, luego revertida | no está en el historial de Radarr |
| Avengers Age of Ultron (2015) | remux verificado 09-03 | ídem |
| Ballerina (2025) | remux verificado 09-03 | ídem |
| Star Wars Episode V (1980) | propedit verificado x2 | ídem |
| Star Wars The Last Jedi (2017) | propedit verificado x2 | ídem |
| Joker Folie à Deux (2024) | remux verificado 09-03 | ídem |
| Blade (1998) | ninguno | ídem |
| Deadpool & Wolverine (2024) | ninguno | ídem |
| F1 (2025) | ninguno | ídem |
| Man of Steel (2013) | ninguno | ídem |
| Avatar Fire and Ash (2025) | ninguno | ídem |
| Pirates of the Caribbean Dead Man's Chest (2006) | ninguno | ídem |

Suman **~439 GB**. Todas comparten `updated_at = 2026-09-16T15:18:41` salvo
Pirates (16:29:20) — un único evento en el tiempo, no 15 eventos separados.

## 3. Los 117 upgrades de Radarr (historial `downloadFolderImported`, 09-07 → 09-20)

114 entraron, remuxearon y quedaron `clean`. **3 quedaron `ready`**: Sonic 2, Top Gun
Maverick, The Northman — el mismo bug que el resto de la tabla de arriba, no algo propio
del lote de upgrades.

Los otros 12 `ready` **no están en el historial de Radarr** — no llegaron por un
import de estos días; ya estaban en la librería.

## 4. Causa

**Confirmado en código** (`app.py:2290-2296`, `scan.py:suggest_tracks`,
`app.py:_enqueue`): el gancho de Radarr (`radarr_hook`) sí encadena
`suggest_tracks` → `_enqueue(remux)` automáticamente. Pero también existe
`POST /api/movies/{id}/suggest` (`app.py:771`), que solo calcula el plan y pone
`status='ready'` — **no lanza el job**. Encolar el remux es un paso aparte
(`POST /api/movies/{id}/jobs`).

Los 15 comparten un único timestamp de segundo exacto: alguien (o una llamada en
bloque) pidió `/suggest` para las 15 a la vez, sin seguir con `/jobs` para
ninguna. `server.log` solo cubre las últimas horas, así que no pude confirmar
quién o qué hizo esa llamada — lo dejo sin verificar.

No es la política (`suggest_tracks` solo pide inglés, español y el idioma
original) ni un fallo del remux: es que el remux **no se lanzó**. El fichero de
Top Gun tiene 24 subtítulos y 3 audios en disco porque nadie lo tocó desde que
Radarr lo trajo.

## 5. Bug aparte, encontrado auditando el resto de `clean` (no pedido, pero real)

`POST /api/movies/{id}/accept` marca `status='clean'` sin comprobar las pistas —
es "la calidad ya es buena, no hace falta remux", pensado para ficheros que ya
llegan limpios de por sí. **The Flash (2023)** se aceptó así con 17 idiomas de
subtítulo de más (bul, chi, cze, dut, est, ger, gre, heb, ind, ita, jpn, kor, lav,
lit, por, rus, slv) todavía en el fichero — mismo síntoma que Top Gun, pero
marcada `clean`, así que no salió en el filtro por `status='ready'`.

Comprobé las otras 37 películas `clean` sin ningún job: todas coinciden con su
idioma original + inglés/español, es decir, `/accept` se usó bien en esos 37.
Solo The Flash es un caso real. +6,3 GB.

## 6. Lo que NO es un problema

- Los 20 títulos `clean` con coreano/japonés/chino/francés/alemán/sueco/danés
  como idioma extra son su idioma original (política: original + eng + spa) —
  correcto por diseño.
- El "4K" en el título global y el subtítulo "Español" sin especificar variante
  son metadata del fichero crudo, no algo que el remux deje así: `build_mkvmerge_remux`
  siempre fija `--title` limpio. Se resuelven solos en cuanto la 15+1 pasen por
  el pipeline.

## 7. Resolución elegida (2026-09-22)

Antonio cancela el remux directo. Reducción de tamaño por Radarr contra el
techo ya decidido en `LIBRARY_STATE.md` (4K 32 Mbps, 1080p 15, SD 8), medido
sobre el bitrate de **vídeo** (tag `BPS`), no el total del contenedor.

Cruzado con las 16 del audit: **solo 2 superan el techo** (Joker Folie à Deux
60,2 y Pirates Dead Man's Chest 42,5). Top Gun Maverick da 27,0 Mbps de vídeo:
ya está dentro del objetivo, no hay nada que reducir. Las otras 14 solo tienen
el problema de pistas; un downgrade no las toca, así que siguen en crudo hasta
que se decida su remux.

Librería completa por encima del techo (21 películas):

| tier | n | GB |
|---|---|---|
| 4K > 32 | 16 | 839 |
| 1080p > 15 | 3 | 45 |
| SD > 8 | 2 | 12 |

4K: Joker Folie à Deux 60,2 · Rocky IV 58,9 · The Forever Purge 58,6 ·
Rocky 56,9 · Conclave 56,8 · Lilo & Stitch (2025) 56,6 · Raiders of the Lost
Ark 54,2 · Rocky II 53,8 · American Sniper 51,2 · Top Gun (1986) 51,1 ·
The Fantastic 4 First Steps 50,3 · 21 Jump Street 49,6 · Snake Eyes 48,6 ·
Pirates Dead Man's Chest 42,5 · Creed II 41,9 · LOTR Return of the King 33,8.
1080p: Dallas Buyers Club 18,2 · Perfect Blue 17,6 · Pursuit of Happyness 15,1.
SD: The Young and the Damned 10,0 · Persona 8,7.

## 7bis. Pendiente de decisión de Antonio (histórico, previo a la resolución de arriba)

**16 películas para remuxear: los 15 `ready` + The Flash.** ~445 GB total, la
mayor individual 67 GB (Joker: Folie à Deux). No he tocado ningún fichero.

No lo lanzo sin confirmación — igual que homelab no toca Radarr sin la mía. Y
mejor esperar al montaje único de Radarr (identidad, ya en marcha) antes de
lanzar re-imports que dependan de él; el remux de estos 16 en sí es una
operación local de media-manager, no pasa por Radarr, así que no tiene el problema
EXDEV que menciona homelab — puedo lanzarlo ya sin esperar esa parte si Antonio
da luz verde.

Con luz verde: `POST /api/movies/{id}/jobs {"kind":"remux","quality":22}` uno a
uno (el runner es global, un job a la vez) para las 16, o lo hago yo por script.
1.8 TB libres en `/srv/storage`, de sobra para la mayor.
